/**
 * 1. create firebase project
 * 2. create web app
 * 3. npm i firebase
 * 4. save firebase config and export default app
 * 5. Build > authentication > enable sign in method
 * 6. create sign up and login route in my project
 * 
**/


/**
 * 1. create a context provider file 
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider
 * 
**/

/**
 * ---------- HOISTING ---------
 * One time per pc install it & login
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 
 * 
 * For each project one time
 * 1. firebase init
 * 2. proceed 
 * 3. hoisting: firebase (change it up and down arrow) then use space bar to select item
 * 4. existing project
 * 5. select the project carefully
 * 6. which project as public directory: dist
 * 7. single page application: y
 * 8. continuous deployment: n
 * 
 * 
 * For every time deploy
 * 1. npm run build
 * 2. firebase deploy
 * 
 * 
 **/